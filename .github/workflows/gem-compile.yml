name: "Package gems with precompiled binaries"
on: push

jobs:
  compile:
    timeout-minutes: 20
    name: "Cross compile the gem on different ruby versions"
    strategy:
      matrix:
        os: ["windows-latest"]
    runs-on: "${{ matrix.os }}"
    steps:
      - name: "Install gperf"
        if: ${{ matrix.os == 'windows-latest' }}
        run: choco install gperf
      - name: "Install gperf"
        if: ${{ matrix.os == 'ubuntu-latest' }}
        run: sudo apt-get -qy install gperf
      - name: "Checkout code"
        uses: "actions/checkout@v5"
      - name: "Setup Ruby"
        uses: "ruby/setup-ruby@v1"
        with:
          ruby-version: "3.0.7"
          bundler-cache: true
          working-directory: "test/fixtures/date"
      - name: "Run easy compile"
        uses: "shopify-playground/edouard-playground/.github/actions/easy_compile@ec-debug-windows-again"
        with:
          step: "compile"
          token: ${{ secrets.GITHUB_TOKEN }} # TODO Remove this before publishing the gem
          working-directory: "test/fixtures/date"
  test:
    timeout-minutes: 30
    name: "Run the test suite"
    needs: compile
    strategy:
      matrix:
        os: ["windows-latest"]
        rubies: ["3.1.7"]
        type: ["cross"]
    runs-on: "${{ matrix.os }}"
    steps:
      - name: "Checkout code"
        uses: "actions/checkout@v5"
      - name: "Setup Ruby"
        uses: "ruby/setup-ruby@v1"
        with:
          ruby-version: "${{ matrix.rubies }}"
          bundler-cache: true
          working-directory: "test/fixtures/date"
      - name: "Run easy compile"
        uses: "shopify-playground/edouard-playground/.github/actions/easy_compile@ec-debug-windows-again"
        with:
          step: "test_${{ matrix.type }}"
          token: ${{ secrets.GITHUB_TOKEN }} # TODO Remove this before publishing the gem
          working-directory: "test/fixtures/date"
  install:
    timeout-minutes: 5
    name: "Verify the gem can be installed"
    needs: test
    strategy:
      matrix:
        os: ["windows-latest"]
    runs-on: "${{ matrix.os }}"
    steps:
      - name: "Setup Ruby"
        uses: "ruby/setup-ruby@v1"
        with:
          ruby-version: "3.4.7"
      - name: "Run easy compile"
        uses: "shopify-playground/edouard-playground/.github/actions/easy_compile@ec-debug-windows-again"
        with:
          token: ${{ secrets.GITHUB_TOKEN }} # TODO Remove this before publishing the gem
          step: "install"
  release:
    permissions:
      id-token: write
      contents: read
    timeout-minutes: 5
    if: ${{ inputs.release }}
    name: "Release all gems with RubyGems"
    needs: install
    runs-on: "ubuntu-latest"
    steps:
      - name: "Setup Ruby"
        uses: "ruby/setup-ruby@v1"
        with:
          ruby-version: "3.4.7"
      - name: "Run easy compile"
        uses: "shopify-playground/edouard-playground/.github/actions/easy_compile@ec-debug-windows-again"
        with:
          step: "release"
          token: ${{ secrets.GITHUB_TOKEN  }}
